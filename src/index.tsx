import React from 'react';
import ReactDOM from 'react-dom';
import AvatarUploader from './pages/AvatarUploader';

ReactDOM.render(
  <React.StrictMode>
    <AvatarUploader />
  </React.StrictMode>,
  document.getElementById('root')
);
